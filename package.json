{
  "name": "baito",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:vercel": "turbo run build:vercel",
    "dev": "bun --env-file=.env turbo run dev",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "clean": "find . -name node_modules -o -name .next -o -name .turbo -o -name dist -type d -prune | xargs rm -rf",
    "check-types": "turbo run check-types",
    "dev:web": "bun --env-file=.env turbo -F @baito/web dev",
    "dev:worker": "bun --env-file=.env turbo -F @baito/worker dev",
    "dev:mastra": "bun --env-file=.env turbo -F @baito/mastra dev",
    "db:seed": "turbo db:seed",
    "db:studio": "turbo db:studio",
    "db:generate": "turbo db:generate",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "db:migrate": "turbo db:migrate",
    "preinstall": "npx only-allow bun",
    "postinstall": "bun db:generate",
    "prepare": "husky"
  },
  "devDependencies": {
    "@ai-sdk/openai": "^3.0.14",
    "@baito/typescript-config": "*",
    "@biomejs/biome": "2.3.11",
    "@mozilla/readability": "^0.6.0",
    "ai": "^6.0.45",
    "husky": "^9.1.7",
    "linkedom": "^0.18.12",
    "ofetch": "^1.5.1",
    "turbo": "^2.6.0",
    "turndown": "^7.2.2",
    "typescript": "5.9.3",
    "ultracite": "7.0.11",
    "zod": "^4.3.5"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*",
    "tests/*"
  ]
}
